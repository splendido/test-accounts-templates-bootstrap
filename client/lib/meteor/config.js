
/*
T9n.map('en', {
	error: {
		accounts: {
			"User not found": "Login Failed",
			"Incorrect password": "Login Failed"
		}
	}
});

T9n.map('it', {
	error: {
		accounts: {
			"User not found": "Accesso Fallito",
			"Incorrect password": "Accesso Fallito"
		}
	}
});
*/

T9n.setLanguage('en');